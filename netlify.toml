# Netlify build configuration for the Astro Staging site.
# -----------------------------------------------------------------------------
# Once you've connected the repo in Netlify, no UI configuration is needed —
# this file is authoritative.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Disable Netlify's HTML/CSS/JS post-processing.
# Astro already produces optimized output. Netlify's post-processor was
# stripping Netlify Forms attributes (data-netlify, hidden form-name input)
# and rewriting our relative URLs to absolute ones, breaking the form flow.
[build.processing]
  skip_processing = true

# Security headers — sensible defaults for a static site
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"

# Long cache for hashed assets, no cache for HTML
[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
